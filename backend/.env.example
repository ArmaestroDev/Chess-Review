PORT=3001
STOCKFISH_PATH=stockfish
STOCKFISH_THREADS=2
STOCKFISH_HASH_MB=128
DEFAULT_DEPTH=14

# RapidAPI key for the chess-puzzles proxy (GET /api/puzzles/:id).
# Server-side only — never expose this in the browser.
RAPIDAPI_KEY=
